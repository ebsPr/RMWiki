import { EpisodeImgPipe } from './episode-img.pipe';

describe('EpisodeImgPipe', () => {
  it('create an instance', () => {
    const pipe = new EpisodeImgPipe();
    expect(pipe).toBeTruthy();
  });
});
